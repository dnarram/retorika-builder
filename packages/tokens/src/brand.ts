/**
 * Retorika's own corporate tokens for the editor interface.
 * Kept deliberately separate from client palettes so client sites cannot
 * accidentally be given Retorika's brand palette.
 */
export const RETORIKA_BRAND: Readonly<Record<string, string>> = Object.freeze({
  blue: "#156FE7",
  darkBlue: "#105CB1",
  fuchsia: "#FF3A72",
  green: "#03D26E",
  ink: "#0F172A",
  white: "#FFFFFF",
});
